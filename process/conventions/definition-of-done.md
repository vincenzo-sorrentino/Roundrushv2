# Definition of Done

A flow is done when all conditions are met:

- Spec file includes required frontmatter and sections.
- Flow and acceptance files are present.
- `links.json` maps to valid prototype route and Figma reference.
- Prototype route renders without runtime errors.
- UI uses `@roundrush/components` and theme/token packages.
- Changes pass spec validation and prototype route checks in CI.
