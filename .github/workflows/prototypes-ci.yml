name: Prototypes CI

on:
  pull_request:
    paths:
      - "prototypes/**"
      - "requirements/**"
      - ".github/workflows/prototypes-ci.yml"

jobs:
  prototype-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
      - run: npm install
      - run: npm run validate:specs
      - run: npm run test --workspace prototypes/app
      - run: node prototypes/_tools/ci/changed-flow-detector.mjs
